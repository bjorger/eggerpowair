import { PageContainer } from "components/page";
import React from "react";

const Projects: React.FC = () => {
    return <PageContainer eyebrow="Erfahrung" title="Unsere Projekte"></PageContainer>;
};

export default Projects;
